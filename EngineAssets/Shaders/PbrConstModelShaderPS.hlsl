// Editor material preview. The forward PBR shader handles constants-only
// materials itself (MaterialParams without MATERIAL_FLAG_USE_TEXTURES), so the
// preview is the same shader.
#include "PbrModelShaderPS.hlsl"
