#pragma once

namespace Ag
{
	// Read cells of a CSV data table (see DataTable.h): as float, int, bool, string or vector, by row name and column
	// name; count rows, list row names, find a row by the value in a column.
	void RegisterDataTableNodes();
} // namespace Ag
