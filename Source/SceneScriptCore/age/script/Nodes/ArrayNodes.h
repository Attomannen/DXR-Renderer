#pragma once

namespace Ag
{
	// Array and Map nodes for the types in ScriptArrays.h (Int, Float, Bool, String, Float3): make, read, search, add,
	// remove, sort, shuffle, loop over them (For Each), and read / write them as variables.
	void RegisterArrayNodes();
} // namespace Ag
