#pragma once

namespace Ag
{
	// The math a controller needs on top of the basic operators: trigonometry, clamp and
	// lerp, vector scale / length / normalize / dot / cross, boolean logic, selection, and
	// turning a yaw into forward and right directions.
	void RegisterMathExtraNodes();
} // namespace Ag
